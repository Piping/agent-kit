[project]
name = "agent-kit"
version = "0.1.0"
description = "Manage personal prompt, skill, and AGENTS assets."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "OpenSpec Maintainers" }
]

[project.scripts]
akit = "agent_kit.cli:main"
